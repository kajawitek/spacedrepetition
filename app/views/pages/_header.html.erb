<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <div class="collapse navbar-collapse" id="navbarText">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <%= link_to "Spaced repetition", root_path, class:"navbar-brand" %>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Log Out", destroy_user_session_path,  data: { turbo_method: :delete }, class:"navbar-brand" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Sign Up", new_user_registration_path, class:"navbar-brand" %>
          </li>
          <li class="nav-item">
            <%= link_to "Log In", new_user_session_path, class:"navbar-brand" %>
          </li>
        <% end %>
      </ul>
      <% if user_signed_in? %>
        <ul class="navbar-nav ml-auto mb-2 mb-lg-0">
          <span class="navbar-text">
            <%= "Hi #{current_user.email}!" %>
          </span>
        </ul>
      <% end %>
    </div>
  </div>
</nav>
